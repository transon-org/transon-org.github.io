# PyScript config. `transon>=0.1.7` is required by the visual editor's forward-projection codec
# (engine recursion-budget fix R-32); the docs rendering uses the same install. micropip honors the
# version specifier when fetching from PyPI (RFC-005 B1).
packages = ["transon>=0.1.7"]
